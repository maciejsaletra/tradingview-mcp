# Strategy Reference (pointer)

Canonical strategy documentation lives in **`PROJECT_CONTEXT.md`** (repo root) — methodology (ICT/SMC), mandatory analysis sequence, pine box/label color filters, indicator list, entry-zone drawing rules, card branding. Read that file directly; this pointer exists only so routine prompts have a stable `context/strategy_reference.md` path to reference per the v2 file-layout spec.

Full v2 operational logic (schedule, confidence model, risk engine, journal schema) lives in **`ROUTINES_V2.md`** (repo root).

Do not duplicate strategy content here — if `PROJECT_CONTEXT.md` changes, this pointer stays valid without edits.
